<div class="comments">
    {{#each this}}
    <section class="comment">

        <div class="text">
            <p>{{comment_text}}</p>
        </div>
        <div class="meta">
            by {{user.username}} on {{ format_date created_at}}
        </div>

    </section>
    {{/each}}
</div>